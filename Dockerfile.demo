FROM node:20-slim
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY tsconfig.json ./
COPY src/ src/
COPY examples/ examples/
RUN npm run build && cp -r examples dist/
EXPOSE 3100
CMD ["npx", "tsx", "examples/shift-exchange-server.ts"]
